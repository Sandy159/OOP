3) в строке с комментарием A выведется John, т.к. была использована функция из пространства имен module1
c D аналогично, выведется James

4)Ошибка возникает из-за коллизии, т.к. в другом пространстве имен содержится одноименная функция.
Чтобы ее не возникало, можно через двоеточие прописать из какого конкретно пространства имен будет использована функция

6)можно перед main прописать using namespace std, тогда не придется каждый раз прописывать std::, но может возникнуть ситуация с той же ошибкой, что и в пункте 4

